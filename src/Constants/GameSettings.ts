// Scaling of the stage
export const stageScale = 4;

// Fog settings used for ThreeJS Fog object
export const fogNear = 3;
export const fogFar = 7;

// Default y position of the objects
export const defaultY = -0.7;
